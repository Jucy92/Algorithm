
#include <stdio.h>
#include <vector>
#include <queue>
#include <algorithm>
#define N 10001
using namespace std;
int chk[1001], val[1001];

int main(){
	freopen("input.txt", "rt", stdin);
	int n, k, i, x;
	queue<int> Q;
	scanf("%d %d", &n, &k);
	
	for(i=1; i<=n; i++){
		scanf("%d", &val[i]);
	}

	
//	/*
	Q.push(1);
//	chk[S]=1;
	
	while(!Q.empty()){
		x=Q.front();
		Q.pop();
		if(x=)
	}
	
	
	
//	*/
	return 0;
}
