#include <stdio.h>
#include <algorithm>
#include <vector>
#include <queue>
using namespace std;
int DFS(int a, int b){
//	if()
	return 0;
}

int main(){
	freopen("input.txt", "rt", stdin);
	int n, m, a, b, i;
	
	scanf("%d %d", &n, &m);
	printf("%d %d\n", &n, &m);
	
	for(i=1; i<=m; i++){
		scanf("%d %d\n", &a, &b);
	}
	scanf("%d %d\n",&a, &b);
	DFS(a, b);
	
	
	return 0;
}

