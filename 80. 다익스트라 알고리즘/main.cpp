#include<stdio.h>
#include<vector>
#include<algorithm>
#define x first
#define y second
using namespace std;	
int ch[30], dist[30];
vector<pair<int, int> > map[30];
int main(){
	freopen("input.txt", "rt", stdin);
	int n, m, i;
	scanf("%d %d", &n, &m);
	
	return 0;
}
