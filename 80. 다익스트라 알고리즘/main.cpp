#include<stdio.h>
#include<vector>
#include<algorithm>
#define x first
#define y second
using namespace std;	
int ch[30], dist[30];
vector<pair<int, int> > map[30];
int main(){
	freopen("input.txt", "rt", stdin);
	int n, m, i, a, b, c;
	scanf("%d %d", &n, &m);
	
	for(i=1; i<=m; i++){
		scanf("%d %d %d", &a, &b, &c);
	}
	
	return 0;
}
